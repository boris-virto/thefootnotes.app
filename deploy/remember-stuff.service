[Unit]
Description=remember_stuff — телеграм-бот и дашборд
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=remember
Group=remember
WorkingDirectory=/opt/remember_stuff
# .env читается самим приложением (load_dotenv) из WorkingDirectory.
ExecStart=/opt/remember_stuff/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

# --- Харденинг (изоляция процесса, близко к контейнерной) ---
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
# Единственное место, куда сервису можно писать — папка с базой и файлами.
ReadWritePaths=/opt/remember_stuff/data
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
RestrictNamespaces=true
LockPersonality=true

[Install]
WantedBy=multi-user.target
